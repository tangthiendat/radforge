# Radforge v1.4.4 Easy Install Release

## Goal

- outcome: publish the restored one-command install flow with the POSIX fresh-install fix as v1.4.4
- success signal: `dev` is committed and pushed, a PR is merged into `main`, v1.4.4 is released from that main commit, and both remote README install paths pass isolated install/uninstall checks

## Scope

In:

- commit `README.md`, `scripts/install.ps1`, `scripts/install.sh`, and this plan on `dev`
- push `dev`, create and merge a ready PR into `main`
- tag the merged main commit as `v1.4.4` and publish a GitHub release
- exercise the remote PowerShell and POSIX README commands against isolated homes

Out:

- deletion or mutation of the historical v1.4.3 release
- changes to provider support, uninstall semantics, or state format
- reintroduction of checksum/versioned bootstrap behavior

Assumptions:

- v1.4.4 is the next patch release after v1.4.3
- the user explicitly approved commit, push, merge, and release

## File Map

Create:

- `docs/plans/2026-07-18-v1.4.4-easy-install-release.md`

Modify:

- `README.md`
- `scripts/install.ps1`
- `scripts/install.sh`

Validate:

- existing parser, syntax, isolated bootstrap, install, and uninstall evidence
- PR merge state and tag target
- remote `main` PowerShell and POSIX install/uninstall smoke checks

Defer:

- native macOS execution because this environment provides PowerShell and Git Bash only

## Tasks

### Task 1: Commit and push the approved scope

Outcome:
Only the restored easy-install files and durable release plan are committed on `dev` and pushed.

Files:

- `README.md`
- `scripts/install.ps1`
- `scripts/install.sh`
- `docs/plans/2026-07-18-v1.4.4-easy-install-release.md`

Validation:
Inspect the staged diff, run `git diff --cached --check`, and verify `dev` is one commit ahead of `origin/dev` before push.

Expected signal:
No unrelated files are staged and the push advances only `dev`.

If it fails:
Inspect worktree scope or remote divergence before committing or pushing.

Checkpoint:
commit and push are approved.

Risk:
low

### Task 2: Merge dev into main

Outcome:
A ready PR captures the change and merges into `main` without overwriting newer work.

Files:

- GitHub PR from `dev` to `main`

Validation:
Confirm PR diff/check state, merge result, and fetched `origin/main` commit.

Expected signal:
The PR is merged and `origin/main` contains the easy-install tree plus the POSIX guard fix.

If it fails:
Stop before tagging and inspect conflicts, checks, or branch protection.

Checkpoint:
merge is explicitly approved.

Risk:
medium because it changes the public install branch.

### Task 3: Release and remotely verify v1.4.4

Outcome:
The merged main commit is tagged and published, and live README commands work.

Files:

- Git tag `v1.4.4`
- GitHub release `v1.4.4`

Validation:
Download and execute the remote main installers for Codex in isolated PowerShell and POSIX homes, then uninstall and verify zero remaining skills.

Expected signal:
Both paths install nine skills, uninstall to zero, and the v1.4.4 tag resolves to the merged main commit.

If it fails:
Do not claim completion; inspect main propagation, tag target, or bootstrap output first.

Checkpoint:
tag and release are explicitly approved.

Risk:
medium because publication is externally visible.

## Validation Strategy

- ordered checks from narrowest to broadest:

  - run: staged diff and existing local installer checks
  - expected: exact approved scope and passing local evidence
  - if it fails: stop before commit

  - run: PR/merge verification
  - expected: main contains the intended tree
  - if it fails: stop before tag

  - run: live remote PowerShell and POSIX lifecycle checks
  - expected: nine skills install and zero remain after uninstall
  - if it fails: inspect remote branch propagation and bootstrap behavior

- lowest useful validation first: yes; irreversible publication follows local and merge checks

## Risks And Approval Points

- the release intentionally restores branch-head remote execution for ease of use
- v1.4.3 remains available as historical release evidence but is no longer the README path
- all external mutations requested here are explicitly approved by the user

## Finalization Decision

- Plan artifact required: yes
- Final path: `docs/plans/2026-07-18-v1.4.4-easy-install-release.md`
- Placeholder check complete: yes

## Next Handoff

- `implement`
